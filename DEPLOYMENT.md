# 🚀 快速部署指南

## 测试结果

✅ **代码检查**: 通过  
✅ **构建测试**: 通过  
✅ **类型检查**: 通过

## 部署到 GitHub Pages

### 步骤 1: 创建 GitHub 仓库

1. 访问 [GitHub](https://github.com) 并登录
2. 点击右上角 **+** → **New repository**
3. 填写仓库信息：
   - Repository name: `metatinker-website` (或你喜欢的名称)
   - 选择 Public
   - **不要**勾选 "Initialize this repository with a README"
4. 点击 **Create repository**

### 步骤 2: 配置 GitHub Pages

1. 进入仓库的 **Settings** 页面
2. 在左侧菜单找到 **Pages**
3. 在 **Source** 部分：
   - 选择 **GitHub Actions**
4. 保存设置

### 步骤 3: 推送代码

在项目目录执行以下命令：

```bash
# 如果还没有初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: MetaTinker website deployment"

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/仓库名.git

# 推送到 main 分支
git branch -M main
git push -u origin main
```

### 步骤 4: 等待自动部署

1. 推送代码后，GitHub Actions 会自动开始构建
2. 在仓库页面点击 **Actions** 标签查看部署进度
3. 部署完成后（通常 2-5 分钟），网站即可访问

### 步骤 5: 访问网站

部署完成后，网站地址为：
```
https://你的用户名.github.io/仓库名/
```

例如，如果仓库名是 `metatinker-website`，地址为：
```
https://你的用户名.github.io/metatinker-website/
```

## ⚙️ 重要配置

### 修改 base 路径

如果仓库名称不是 `metatinker-website`，需要修改 `vite.config.ts`：

```typescript
base: process.env.NODE_ENV === 'production' ? '/你的仓库名/' : '/',
```

然后重新构建和推送。

## 🔍 验证部署

部署成功后，检查以下内容：

- [ ] 网站可以正常访问
- [ ] 所有图片正常显示
- [ ] 导航链接正常工作
- [ ] 表单功能正常
- [ ] 响应式布局正常
- [ ] 动画效果正常

## 📝 后续更新

每次更新代码后，只需：

```bash
git add .
git commit -m "更新描述"
git push
```

GitHub Actions 会自动重新部署。

---

**需要帮助？** 查看详细文档：`docs/deployment.md`

